#include <iostream>
#include "Splaytree.h"

int main() {
    
    //Ignore lines 7 through 12. For testing purposes.
    Splaytree test;

    test.insert((2));
    test.insert((1));
    test.insert((3));
    test.search(4);

    return 0;
}
